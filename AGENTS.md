# Repository Rules

- No authored source file should exceed 400 lines. Generated lockfiles and
  generated platform declarations are exempt.
- Do not use `as unknown` or `as any` in any code.
- After implementation, all tests and checks must pass before claiming any code implementation task is complete.

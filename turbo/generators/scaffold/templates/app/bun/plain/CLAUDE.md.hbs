# @repo/{{ packageName }}

{{#if description}}{{ description }}{{else}}A Bun application.{{/if}}

## Scripts

- `bun run start` — run the app once
- `bun run dev` — run with watch mode
- `bun run check` — biome lint + `tsc --noEmit`

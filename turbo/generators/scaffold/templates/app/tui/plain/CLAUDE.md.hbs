# @repo/{{ packageName }}

{{#if description}}{{ description }}{{else}}A Bun terminal UI application.{{/if}}

## Scripts

- `bun run start` — run the TUI
- `bun run dev` — run with watch mode
- `bun run check` — biome lint + `tsc --noEmit`

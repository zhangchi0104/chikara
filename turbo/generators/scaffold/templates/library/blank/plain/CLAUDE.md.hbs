# @repo/{{ packageName }}

{{#if description}}{{ description }}{{else}}A TypeScript library.{{/if}}

## Scripts

- `bun run build` — TypeScript build to `dist/`
- `bun run check` — biome lint + `tsc --noEmit`
- `bun test` — run tests
